test6/
│
├── app/
│   ├── Validators/          ← バリデーション
│   │     AccountsValidator.php
│   │     AuthValidator.php
│   │     VoucherValidator.php
│   │
│   ├── controllers/        ← HTTP処理
│   │       └lib
│   │           auth.php
│   │       AccountsController.php
│   │       AuthController.php
│   │       VoucherController.php
│   │       HomeController.php
│   │       LogoutController.php
│   │       MenuController.php
│   │
│   ├── services/           ← 業務ロジック
│   │         └lib
│   │             HomeLib.php
│   │       AccountsService.php
│   │       AuthService.php
│   │       HomeService.php
│   │       VoucherService.php
│   │       ReportService.php
│   │
│   ├── repositories/       ← DB操作
│   │       UserRepository.php
│   │       voucherRepository.php
│   │       AccountRepository.php
│   │       HomeRepository.php
│   │       ReportRepository.php
│   │       
│   │       
│   │       
│   ├── DTO/                ← データ構造
│   │       AccountsDto.php
│   │       VoucherDto.php
│   │       Constants.php
│   │       HmSvcDTO.php
│   │       LoginDTO.php
│   │       PdoDTO.php
│   │       RegisterDTO.php
│   │     
│   │
│   └── models/             ← データ構造
│           User.php
│           Voucher.php
│           VoucherDetail.php
│           Account.php
│
├── config/
│     bootstrap.php
│     db.php
│     session.php
│
├── lib/
│     helpers.php
│
├── public/                 ← DocumentRoot
│     index.php
│
├── views/   
│     ├  accounts/
│     │     add.php
│     │
│     ├  home/
│     │     HomeView.php
│     │     index.php
│     │  
│     ├  lib/
│     │     ProcSlct.php
│     │  
│     ├  Accounts/
│     │     AccountsView.php
│     │
│     ├  auth/
│     │     login.php
│     │     register.php
│     │
│     ├  voucher/
│     │      create.php
│     │      edit.php
│     │      index.php
│     │      list.php
│     │
│     └  reports/
│               ledger.php
│               trial_balance.php
│               profit_loss.php
│               balance_sheet.php
│
└── storage/
      backup/
      logs/



1. プロレベル構造（4層）

一般的な業務システムは次の構造です。

Controller
   ↓
Service
   ↓
Repository
   ↓
Model
   ↓
Database

役割はこうです。

層	役割
Controller	HTTP処理 (POST / GET)
Service	業務ロジック
Repository	DB操作
Model	データ構造

資産     → 借方がプラス
負債     → 貸方がプラス
純資産   → 貸方がプラス

ログ出力について
デバッグログ出力    php     file_put_contents('/var/www/html/test6/public/debug.log', "メソッド通ったよ！\n", FILE_APPEND);
                            file_put_contents(__DIR__ . '/../../debug.log', "メソッド通ったよ！\n", FILE_APPEND);

処理続行、停止確認メッセージ
                                    <button name="VcrDelete" type="submit" 
                                        onclick="return confirm('この伝票を削除すると、紐づく明細データもすべて削除されます。\n本当に削除してもよろしいですか？');" 
                                        class="btn btn-danger"
                                        value="<?= h('VcrDelete') ?>">伝票削除
                                    </button>

mysqlでdelete insert create 操作を行った後、成功したかどうかを確認する方法は、PDOの場合はexecute()の返り値を確認するか、rowCount()メソッドを使用して影響を受けた行数を確認することが一般的です。
                            行数が入る
          mysql rowCount()   
          
          

javascript Alert確認メッセージ

		echo "	<script>
    			    alert('セッションの有効期限が切れたか、不正な操作が行われました。\\n再度ログインしてください。');
        			window.location.href = 'index.php?route=login';
    			</script>
		";
      exit;

      
       <script>
            alert('伝票が正常に削除されました。');
      </script>


ｓｑｌ
   ALTER TABLE journal_details MODIFY id INT NOT NULL AUTO_INCREMENT ;

   alter table journal_details add line_no int after voucher_id;
   ALTER TABLE journal_details ADD jd_summary VARCHAR(255) AFTER voucher_id;

   テーブル行　全件にカラムに同じデータを設定する
      update accounts set user_id='sunkusminamisuita1@gmail.com';




ToDo 2026/06/19

    現在の状況は

　各試算表出力　　　一次テスト完了

　仕分け伝票入力　　一次テスト完了

　仕分け伝票修正　　一次テスト完了                     

　仕分け伝票行削除　一次テスト完了

　店舗情報　作成　　コーディング完了　テスト中　バグあり
　
　店舗情報　修正    コーディング完了　テスト中　バグあり

　店舗情報　削除　　コーディング完了

　店舗情報　閉店処理　コーディング完了

　一連のデータを連結テスト
      作成した仕分け伝票の修正　連結テスト完了
      作成した仕分け伝票の削除　連結テスト完了
      作成、修正した仕分け伝票の試算表出力　連結テスト完了

###  注意  ###
   ログイン、アカウント関連　初期作成分 syntax logicalエラー多数
   再度見直し必要


今後の機能強化

　科目テーブルをユーザごとに使い分けする。
　科目テーブル
　       作成
         修正
   仕分け伝票作成画面
      左下　作成した仕分け伝票　表示



ここから以降はlaravel環境へ以降

　開発済みphpを移行



　損益計算書、貸借対照表　出力

  仕分け伝票作成画面
      右ページ　損益計算書、貸借対照表　切り替え表示

　消費税計算書　出力

　各勘定元帳　出力

　減価償却　償却台帳 

useridの取得
   $userId = getLoginUserId();



array_filter countを使用したロジック

        foreach ($Dto->AcctAltTbl as $key => $Row) {
            // 配列の中に現在のタイプが含まれているかチェックする
            if (in_array($Row['type'], $Dto->AccountsType, true)) {
                $Dto->AcctAltTbl[$key]['errmsg'] = "OK";
            } else {
                $Dto->AcctAltTbl[$key]['errmsg'] = "貸借種別は'収益'か'費用'か'資産'か'負債'か'資本'以外は入力できません。";
            }

            // 🌟 2. 自分自身も含めて「AcctAltTbl」全体から同じデータを検索する
            // ※ すでに削除チェック（del == 'On'）がついている行は比較対象から外すとより正確になります
            if (($Row['edittype'] ?? '') !== '削除') {
            
                $sameRows = array_filter($Dto->AcctAltTbl, function($searchRow) use ($Row) {
                    // 削除予定の行はカウント対象外にする
                    if (($searchRow['edittype'] ?? '') === '削除') {
                        return false;
                    }
                    // 「名前」と「種別」が一致する行を生き残らせる
                    return $searchRow['name'] === $Row['name'] && $searchRow['type'] === $Row['type'];
                });

                // 🌟 3. count() で生存した件数を取得し、2件以上なら重複エラー！
                if (count($sameRows) >= 2) {
                    $Dto->AcctAltTbl[$key]['errmsg'] = "このデータはすでに登録（重複）されています。";
                }
            }
            //XXX
        }


// アロー関数(php7.4移行では下記肝略記述が使える。)を使うと use を書かなくても外の変数 $Row を自動で参照できます
$sameRows = array_filter($Dto->AcctAltTbl, fn($searchRow) => 
    (($searchRow['edittype'] ?? '') !== '削除') &&
    ($searchRow['name'] === $Row['name']) &&
    ($searchRow['type'] === $Row['type'])
);


日本語文字数をチェックするロジック
            $trimmedName = trim(mb_convert_kana($Row['name'] ?? '', "s", "UTF-8"));//全角スペースを半角に変換してトリム
            if ($trimmedName === '') {
                $Dto->AcctAltTbl[$key]['errmsg'] = "勘定科目名は必須です。";
                $ErrFlg++;
                continue;
            }

            if (mb_strlen($trimmedName, 'UTF-8') > 50) {                          //文字数チェック　半角も全角も1文字としてカウントする
                $Dto->AcctAltTbl[$key]['errmsg'] = "勘定科目名は50文字以内で入力してください。";
                $ErrFlg++;
                continue;
            }


未検討事項
    ◯店舗連結決算をユーザーid内で複数店舗を管理するか？　１ユーザーid１店舗で、ユーザーidを複数作成するか？　どちらが良いか検討する必要あり。


今後の課題
    ◯全コードで未使用変数、未使用メソッドの削除、デバッグコードの削除
    ◯デバッグモード機能を組み込む
    ◯初期作成コードがMVC構造を逸脱している箇所が多いので、MVC構造に沿ったコードに修正する必要あり。
    ◯決算書、貸借対照表、消費税計算書、各勘定元帳、減価償却償却台帳の出力を作成する必要あり。


urlを取得

    // 1. プロトコル（http:// または https://）の判定
    $protocol = (!empty($_SERVER['HTTPS']) && $_SERVER['HTTPS'] !== 'off') ? "https://" : "http://";

    // 2. ドメイン名（例: localhost や example.com）の取得
    $host = $_SERVER['HTTP_HOST'];

    // 3. パスとクエリ文字列（例: /index.php?id=5）の取得
    $requestUri = $_SERVER['REQUEST_URI'];

    // すべてを結合して現在のURLを完成
    $currentUrl = $protocol . $host . $requestUri;

    echo $currentUrl;


 💡 トグル（切り替え）コメント

    現在選択している行にコメントがない場合はコメントアウトし、すでにある場合は解除します。一番よく使います。

        キー操作: Ctrl + /

💡 ブロックコメント（範囲コメント）

    行全体ではなく、選択した文字列の「その部分だけ」をピンポイントでコメントではさみます。（PHPの /* ... */ など）

        キー操作: Shift + Alt + A

    2. 選択範囲を一括で「Tab（インデント）」操作する

複数行のコードを選んで、右にずらしたり左に戻したりする操作です。行のどこを選択していても（行の途中でも）、選択範囲に含まれる行全体が丸ごと動くので非常に便利です。
    💡 一括でインデントを入れる（右にずらす）

        キー操作: Tab （または Ctrl + ]）

            行を選択した状態で Tab キーを押すだけで、選択されたすべての行の先頭に一斉にスペース/タブが挿入されます。

    💡 一括でインデントを戻す（左にずらす）

        キー操作: Shift + Tab （または Ctrl + [）

            インデントを下げすぎて左に戻したいときは、Shift を押しながら Tab を押します。


正規表現　

PHPの正規表現は、特定の文字列のパターンを検索や置換するための記号のルールです。
検索パターンを「/ (スラッシュ)」で囲み、専用の関数（preg_match や preg_replace など）の引数として渡して使います。

関数と処理内容
　関数　　　 	処理 　　
preg_match() 	正規表現による検索を行い、最初に一致した文字列を取得
preg_match_all() 	正規表現による検索を行い、一致した文字列をすべて取得す
preg_replace() 	正規表現による検索を行い、一致した文字列を他の文字列に置換
preg_grep() 	配列に対して正規表現による検索を行い、一致した要素を配列で返却
検索方法と書き方、意味
　　 検索方法　　　 	　　書き方　　 	　　　意味　　　
部分一致 	/文字列/ 	/Orange/（「Orange」が含まれているかどうか）
前方一致 	/^文字列/ 	/^090/（「090」で始まっているかどうか）
後方一致 	/文字列$/ 	/。$/（「。」で終わっているかどうか）
完全一致 	/\A文字列\z/ 	/\A侍太郎\z/（「侍太郎」と完全に一致しているかどうか）
または 	` 	`
小文字のアルファベット 	[a-z] 	/[a-z]/（小文字が含まれているかどうか）
大文字のアルファベット 	[A-Z] 	/^[A-Z]/（大文字で始まっているかどうか）
数字 	[0-9] 	/[0-9]$/（数字で終わっているかどうか）
英数字 	[a-zA-Z0-9] 	/\A[a-zA-Z0-9]\z/（英数字と完全に一致しているかどうか）
改行以外のすべての文字 	. 	/.+/（改行以外の1文字以上の文字が含まれているかどうか）
0回以上の繰り返し 	* 	/\Aあ.*り$\z/（「あ」で始まって「り」で終わる文字列と完全に一致しているかどうか）
1回以上の繰り返し 	+ 	/[a-zA-Z0-9]+/（1文字以上の英数字が含まれているかどうか）
n回の繰り返し 	{n} 	/[0-9]{4}/（4桁の数字が含まれているかどうか）
グループ化 	() 	(○○○)-(●●●●)-(■■■■)＝
2-$3



array_column
PHPの array_column() は、多次元配列（またはオブジェクトの配列）の中から「特定のかラム（列）の値だけを抜き出したり、特定のキーで配列を再構築したりできる」非常に便利で強力な組み込み関数です。

SQLの SELECT column_name FROM table や、Excelの「特定の列だけをコピペする」操作をイメージすると分かりやすいです。
💡 基本的な機能（2つの使い方）

    特定の列だけを抽出して1次元配列にする

    特定の列を「キー」にして配列を作り直す（★前回の店舗コード検索で使った手法）

1. 基本構文
PHP

array_column(array $array, int|string|null $column_key, int|string|null $index_key = null): array

    第1引数（必須）： 対象となる多次元配列

    第2引数（必須/null可）： 抜き出したい列のキー名（PHP 7.0以降は null も指定可能）

    第3引数（任意）： 新しい配列の「キー（インデックス）」にしたい列のキー名

2. 使用例と解説

以下のような「店舗データの配列」を例にして解説します。
PHP

$shops = [
    ['id' => 101, 'shop_code' => 'S001', 'shop_name' => '東京店'],
    ['id' => 102, 'shop_code' => 'S002', 'shop_name' => '大阪店'],
    ['id' => 103, 'shop_code' => 'S003', 'shop_name' => '名古屋店'],
];

パターン①：特定の列だけを抜き出す（1次元化）

「店舗名（shop_name）だけのリストが欲しい」という場合です。
PHP

$shopNames = array_column($shops, 'shop_name');

print_r($shopNames);

【出力結果】
PHP

Array
(
    [0] => 東京店
    [1] => 大阪店
    [2] => 名古屋店
)

    使いどころ：
    データベースに渡す用のIDリスト（[101, 102, 103]）を作りたい時や、in_array() で存在チェックをしたい時などに foreach を書かずに一発で作成できます。

パターン②：特定の列を「キー」にして配列表を再構築する（★重要！）

第3引数に列名を渡すと、その列の値を配列のインデックス（キー）にできます。
PHP

$shopCodes = array_column($shops, 'shop_name', 'shop_code');

print_r($shopCodes);

【出力結果】
PHP

Array
(
    [S001] => 東京店
    [S002] => 大阪店
    [S003] => 名古屋店
)

パターン③：全データを保持したまま、特定の列をキーにする（第2引数を null）

第2引数に null を渡し、第3引数にキーにしたい列名を指定すると、データ構造（全項目）を保持したままキーだけを付け替えることができます。
PHP

// 第2引数を null にすることで、値（1行分のデータ）はそのまま保持される
$indexedShops = array_column($shops, null, 'shop_code');

print_r($indexedShops);

【出力結果】
PHP

Array
(
    [S001] => Array ( [id] => 101, [shop_code] => S001, [shop_name] => 東京店 )
    [S002] => Array ( [id] => 102, [shop_code] => S002, [shop_name] => 大阪店 )
    [S003] => Array ( [id] => 103, [shop_code] => S003, [shop_name] => 名古屋店 )
)

🌟 なぜパターン③（先ほどのコード）が強力なのか？

通常、多次元配列から「店舗コード S002 のデータ」を探す場合、foreach で先頭から1行ずつ探す（O(N)の計算量）必要があります。

しかし、array_column($shops, null, 'shop_code') を使ってキー化（辞書化）しておくと、以下のように isset() やキー指定だけで一発（一瞬）でデータを取り出せるようになります。
PHP

// foreachを使わずに、直接指定・検索できる！
if (isset($indexedShops['S002'])) {
    echo $indexedShops['S002']['shop_name']; // 「大阪店」が瞬時に取れる
}

先ほどリファクタリングしたコードで2重ループ（foreach の中の foreach）を消すことができたのは、この array_column による高速な辞書化を利用したためです。






mysql  insert into 構文
            $sql = "INSERT INTO shops (
                user_id,
                shop_code,
                shop_name,
                open_date,
                closed,
                closed_date,
                summary,
                created_at,
                edittype
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)";


php true false

    false とみなされる主な値（空の値）
    PHPでは、ほかのデータ型も自動で true か false に変換されます。次の値はすべて false として扱われます： 

        数字の 0 と -0
        小数の 0.0 と -0.0
        空の文字 "" や文字列の "0"
        中身が空の配列 array()
        特別な値 null 



shopController.php
ShopsDto.php
ShopsRepository.php
shopsService.php
ShopsValidator.php
ProcSlct.php
ShopsView.php
